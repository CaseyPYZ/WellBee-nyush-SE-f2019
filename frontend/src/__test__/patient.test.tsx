import React from "react";
import { render, fireEvent, waitForElement } from "@testing-library/react";
import App from '../index'

describe("<App />", () => {
    test("Still figuring out", async () => {
        // 
    });
});